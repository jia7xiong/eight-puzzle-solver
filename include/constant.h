#ifndef __CONSTANT_H__
#define __CONSTANT_H__

#define N 9 // Easy to generate an n_Puzzle solver by changing "#define N 9" to "#define N n+1".
#define length  int(sqrt(N)) // The number of tiles in one row or column 
#define L 4000*N // As long as your computer performance is affordable, as for L no more than (N!), the greater, the better.

#endif